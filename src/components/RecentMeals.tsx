import { StyleSheet, View, Text } from "react-native";
import { globalStyles } from "@/styles/global";
import MealItem from "./MealItem";

const RecentMeals = () => {
  return (
    <View>
      <Text>RecentMeals</Text>
    </View>
  );
};

export default RecentMeals;
